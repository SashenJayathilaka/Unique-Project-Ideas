/** @type {import('next').NextConfig} */
const nextConfig = {
  /*   experimental: {
    serverActions: true,
  }, */
  images: {
    domains: ["qrfelesatfsfyhtmcxmc.supabase.co"],
  },
};

export default nextConfig;
